{
  "name": "@frozik/signaling",
  "version": "1.0.0",
  "private": true,
  "description": "y-webrtc-style signaling server — generic pub/sub relay for WebRTC SDP handshake (used by Retro and Conf features)",
  "type": "module",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node dist/server.js",
    "types": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@frozik/typescript-config": "workspace:*"
  }
}
