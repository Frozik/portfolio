/**
 * Pixel gaps used by the sudoku field/controls grid.
 * Must stay in sync with the Tailwind utilities applied to the corresponding
 * elements: gap-1 / p-1 (4px), gap-px (1px), mt-2.5 (10px).
 */
export const FIELD_GAP_PX = 4;
export const FIELD_GROUP_GAP_PX = 1;
export const FIELD_CONTROLS_MARGIN_PX = 10;
