[auth]
token_expiry_warning_seconds = 1
clock_tolerance_seconds = 0

[auth.jwks]
fetch_max_attempts = 1
fetch_timeout_ms = 100

[room]
response_gather_timeout_ms = 100

[signal]
max_publish_per_second_per_socket = 1000

[logging]
level = "warn"
